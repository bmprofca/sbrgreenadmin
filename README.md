# SBRGREEN Admin

Manage all website content stored in MySQL.

## Run

```bash
cd ADMIN
npm install
npm start
```

Runs on [http://localhost:3001](http://localhost:3001).

## Login

- Username: `admin`
- Password: `Admin@123`

API URL is configured in `.env` as `REACT_APP_API_URL`.
